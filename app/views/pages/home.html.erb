<div class="container">
	<h1>👨‍💻 👨🏾‍💻 Bienvenue sur notre super🏋🏿 site 👩🏿‍💻 👩🏼‍💻</h1>

	<h1>Users</h1>

<% @users.each do |user| %>
    <h2> id: <%= user.id %></h2>
	<h2><%= user.email %></h2>
	<a href="<%= user_path(user.id)%>" target="_blank">see profil</a> 
<% end %>

<!-- if user is login -->

  <% if current_user %>
    	<p> 
		<%= link_to  'la réponse à la question de l\'univers.', '/pages/secret_page', target:"_blank" %>		
	</p>
  <% else %>  
    <p> connecte toi pour voir la reponse à la question de l'univers</p>
  <% end %>

</div>